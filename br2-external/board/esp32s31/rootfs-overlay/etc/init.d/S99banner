#!/bin/sh
# SPDX-License-Identifier: MIT
# make monitor waits for this line.  If you change the text, also change
# --success-pattern in the Makefile.

[ "$1" = start ] || exit 0

echo
echo "=== ESP32-S31 Linux / Buildroot ==="
echo "Console: ttyS0        Panel: tty1 (dark while the loader keeps JTAG)"
echo "Wi-Fi:   wpa_passphrase <ssid> <passphrase> >> /etc/wpa_supplicant.conf"
echo "         /etc/init.d/S40wifi restart; wpa_cli status"
echo
